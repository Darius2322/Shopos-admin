// Retired: the sync status now lives in the phone \"More\" menu (and the desktop sidebar).
// Kept as an empty component only so any old import still compiles.
export function SyncButton(_props: { compact?: boolean; floating?: boolean }) {
  return null;
}
